// p = parent node
// e = leaf node for white
// f = leaf node for black
// 1024 pixels, each level /4 per f
// better done in python

#include<iostream>
#include<string>

struct quadtree{
	
}

int main(){
	
	int N;
	string tree1, tree2, treeOut;
	cin >> N;
	
	for(int i = 0; i < N; i++){
		cin >> tree1 >> tree2;
		
		
	}
	
	return 0;
}